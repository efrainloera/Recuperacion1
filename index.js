//Map es una función que te permite transformar los 
//elementos de una lista y que devuelve una nueva lista con los elementos transformados.


const ejemploUno = listaOriginal.map(function callback(elementoActual, index) {
    return /* Elemento devuelto de nuevaLista */;
  });